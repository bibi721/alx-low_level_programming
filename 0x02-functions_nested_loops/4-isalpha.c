#include "main.h"
/**
 * _isalpha - This ckecks whether a letter or lowercase or uppercase
 * @c: single letter input
 * Return: 0 and 1.
 *
 */
int _isalpha(int c)
{
	if (c >= 'a' && c <= 'z' || c >= 'A' && c <= 'Z')
		return (0);
	else
		return (0);
}
