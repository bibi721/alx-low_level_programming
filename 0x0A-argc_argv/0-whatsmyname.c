#include "main.h"
#include <stdio.h>
/**
 * main - a program that prints its name
 * @argc: counter
 * @argv: arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
